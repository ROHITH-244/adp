// Example data to populate the boxes
let shortlistedCandidates = 50;
let totalJobPostings = 10;
let totalApplications = 200;

// Populate the box with the data
document.getElementById("shortlisted-count").textContent = shortlistedCandidates;
document.getElementById("job-posting-count").textContent = totalJobPostings;
document.getElementById("application-count").textContent = totalApplications;
